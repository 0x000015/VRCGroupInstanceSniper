# VRCGroupInstanceSniper

Type `npm i` in the root directory to install all necessary packages
then run `node main.js` and go through the setup steps

Requires [NodeJS (preferably LTS)](https://nodejs.org/en)